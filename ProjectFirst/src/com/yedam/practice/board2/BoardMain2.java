package com.yedam.practice.board2;

public class BoardMain2 {
	public static void main(String[] args) {
		
		startBoard nboard = new startBoard(); 
		nboard.startBoard();
		//List<Board> list = new ArrayList<>();
				//BoardService service = new BoardServiceImpl();
				//sout(제목입력);
				//String title = scn.nextLine();
				//......
				//Board board = new Board()'
				//service.insertBoard(list, board);
				//insertBoard에 db역할을 하는 list를 넣어야함.
				
				//public interface BoardService{
				//void insertBoard(List<Board> list, Board board);
				//void updateBoard(List<Board> list, Board board);
				
				//
	}
}
